<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>우리동네 카페 - 게시글 목록</title>
  <style>
    :root {
      --naver-green: #03c75a;
      --line: #e5e7eb;
      --text: #202936;
      --muted: #667085;
      --bg: #f5f7fb;
      --card: #ffffff;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: "Malgun Gothic", "Apple SD Gothic Neo", sans-serif;
      background: var(--bg);
      color: var(--text);
    }
    .top {
      background: var(--card);
      border-bottom: 1px solid var(--line);
      padding: 14px 18px;
      position: sticky;
      top: 0;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .left-box {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .brand {
      font-size: 19px;
      font-weight: 800;
      color: var(--naver-green);
      text-decoration: none;
    }
    .current-user {
      color: #475569;
      font-size: 13px;
      font-weight: 700;
    }
    .auth-menu {
      display: flex;
      gap: 8px;
    }
    .auth-menu a, .auth-menu button {
      border: 1px solid var(--line);
      border-radius: 10px;
      padding: 6px 10px;
      text-decoration: none;
      color: #0f172a;
      font-size: 13px;
      font-weight: 700;
      background: #fff;
      cursor: pointer;
    }
    .wrap {
      max-width: 920px;
      margin: 22px auto;
      padding: 0 14px;
    }
    .board-head {
      background: var(--card);
      border: 1px solid var(--line);
      border-radius: 14px;
      padding: 18px;
      margin-bottom: 14px;
    }
    .board-head h1 {
      margin: 0;
      font-size: 24px;
    }
    .board-head p {
      margin: 8px 0 0;
      color: var(--muted);
      font-size: 14px;
    }
    .post-list {
      padding: 0;
      list-style: none;
      margin: 0;
      display: grid;
      gap: 10px;
    }
    .post-item {
      background: var(--card);
      border: 1px solid var(--line);
      border-radius: 12px;
      padding: 14px;
      display: flex;
      justify-content: space-between;
      gap: 10px;
      align-items: flex-start;
    }
    .post-main {
      flex: 1;
      min-width: 0;
    }
    .post-title {
      color: var(--text);
      text-decoration: none;
      font-weight: 700;
      font-size: 16px;
    }
    .post-title:hover { color: var(--naver-green); }
    .post-content {
      margin-top: 7px;
      color: var(--muted);
      font-size: 14px;
      line-height: 1.5;
    }
    .post-vote {
      border: 1px solid var(--line);
      border-radius: 10px;
      padding: 8px 10px;
      min-width: 90px;
      text-align: right;
      background: #fff;
      font-size: 12px;
      color: #475569;
      font-weight: 700;
    }
    .post-vote div + div {
      margin-top: 4px;
    }
    .tab-row {
      display: flex;
      gap: 8px;
      margin-bottom: 12px;
    }
    .tab {
      border: 1px solid var(--line);
      border-radius: 10px;
      padding: 6px 10px;
      text-decoration: none;
      font-size: 13px;
      font-weight: 700;
      color: #0f172a;
      background: #fff;
    }
    .tab.active {
      background: var(--naver-green);
      border-color: var(--naver-green);
      color: #fff;
    }
    .pager {
      margin-top: 12px;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .pager a, .pager span {
      border: 1px solid var(--line);
      background: var(--card);
      border-radius: 10px;
      padding: 6px 10px;
      text-decoration: none;
      color: #0f172a;
      font-size: 13px;
      font-weight: 700;
    }
    .write-btn {
      display: inline-block;
      margin-top: 10px;
      border: 1px solid var(--naver-green);
      background: var(--naver-green);
      color: #fff;
      border-radius: 10px;
      padding: 8px 12px;
      font-weight: 700;
      cursor: pointer;
      text-decoration: none;
      font-size: 14px;
    }
    .best-box {
      background: #ecfdf3;
      border: 1px solid #86efac;
      border-radius: 14px;
      padding: 14px;
      margin-bottom: 12px;
    }
    .best-title {
      font-size: 13px;
      color: #166534;
      font-weight: 800;
      margin-bottom: 6px;
    }
    .best-link {
      color: #0f172a;
      text-decoration: none;
      font-weight: 800;
    }
    .best-meta {
      margin-top: 6px;
      color: #475569;
      font-size: 13px;
      font-weight: 700;
    }
    .all-head {
      margin-top: 14px;
      margin-bottom: 8px;
      font-size: 18px;
    }
  </style>
</head>
<body>
  <div class="top">
    <div class="left-box">
      <a class="brand" href="/boards">우리동네 카페</a>
      <span id="currentUserName" class="current-user">사용자</span>
    </div>
    <div class="auth-menu">
      <a id="loginLink" href="/login">로그인</a>
      <a href="/join">회원가입</a>
      <button id="logoutBtn" type="button">로그아웃</button>
    </div>
  </div>
  <main class="wrap">
    <div class="tab-row">
      <a class="tab active" href="/boards">글쓰기 게시판</a>
      <a class="tab" href="/image-boards">이미지 게시판</a>
    </div>
    <section class="board-head">
      <h1>글쓰기 게시판</h1>
      <p>동네 소식과 일상을 자유롭게 나눠보세요.</p>
      <a class="write-btn" href="/boards/write-form">글쓰기</a>
    </section>
    {{#bestPost}}
    <section class="best-box">
      <div class="best-title">베스트 게시글</div>
      <a class="best-link" href="{{url}}">{{title}}</a>
      <div class="best-meta">좋아요 {{likeCount}} · 싫어요 {{dislikeCount}}</div>
    </section>
    {{/bestPost}}
    <h2 class="all-head">페이지 목록</h2>
    <ul class="post-list">
    {{#boards}}
    <li class="post-item">
      <div class="post-main">
        <a class="post-title" href="/boards/{{id}}">{{title}}</a>
        <div class="post-content">{{content}}</div>
      </div>
      <div class="post-vote">
        <div>좋아요 {{likeCount}}</div>
        <div>싫어요 {{dislikeCount}}</div>
      </div>
    </li>
    {{/boards}}
    {{^boards}}
    <li class="post-item">등록된 게시글이 없습니다.</li>
    {{/boards}}
    </ul>
    <div class="pager">
      {{#hasPrev}}
      <a href="/boards?page={{prevPage}}">이전</a>
      {{/hasPrev}}
      <span>{{displayPage}} 페이지</span>
      {{#hasNext}}
      <a href="/boards?page={{nextPage}}">다음</a>
      {{/hasNext}}
    </div>
  </main>
  <script>
    function parseJwt(token) {
      try {
        const payload = token.split(".")[1];
        const normalized = payload.replace(/-/g, "+").replace(/_/g, "/");
        return JSON.parse(atob(normalized));
      } catch (e) {
        return null;
      }
    }

    const token = sessionStorage.getItem("accessToken");
    if (token) {
      const payload = parseJwt(token);
      if (payload && payload.sub) {
        const isAdmin = payload.roles && payload.roles.indexOf("ADMIN") !== -1;
        document.getElementById("currentUserName").textContent = isAdmin ? payload.sub + " (ADMIN)" : payload.sub;
        document.getElementById("loginLink").style.display = "none";
      }
    }

    document.getElementById("logoutBtn").addEventListener("click", function () {
      const token = sessionStorage.getItem("accessToken");
      if (!token) {
        alert("비로그인 상태입니다. 먼저 로그인해주세요.");
        return;
      }

      // 로그인 상태에서만 토큰을 삭제하고 로그아웃을 처리한다.
      sessionStorage.removeItem("accessToken");
      document.cookie = "accessToken=; path=/; Max-Age=0";
      alert("로그아웃 되었습니다.");
      location.href = "/boards";
    });
  </script>
</body>
</html>
